const path = require('path');
const { model } = require('mongoose');

const signupController = {};

signupController.doSomething = (req, res, next) => {

}

// add new user to database
// bcrypt password

module.exports = signupController;